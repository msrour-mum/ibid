import { AddBidDirective } from './add-bid.directive';

describe('AddBidDirective', () => {
  it('should create an instance', () => {
    const directive = new AddBidDirective();
    expect(directive).toBeTruthy();
  });
});
